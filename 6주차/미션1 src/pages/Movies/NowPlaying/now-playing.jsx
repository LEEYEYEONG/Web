// import React from 'react';
// import CardList from './CardList'; // CardList 컴포넌트를 import 합니다.
// import Card from './Card'; // Card 컴포넌트를 import 합니다.

// const NowPlayingPage = ({ movies }) => {
//     if (!movies || !movies.data) {
//         return <div><h1 style={{ color: 'white' }}>에러 중</h1></div>
//     }

//     return (
//         <S.CardList>
//             {movies.data?results.map((movie) => (
//                 <Card key={movie.id} movie={movie} />
//             ))}
//         </S.CardList>
//     );
// };

// export default NowPlayingPage;